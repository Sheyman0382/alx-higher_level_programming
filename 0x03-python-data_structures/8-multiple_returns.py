#!/usr/bin/python3
def multiple_returns(sentence):
    if sentence > 0:
	ist_leta = (sentene[0],)
    else:
        ist_leta = None
    length = (len(sentence),)
    ist_leta = (sentene[0],)
    return length + ist_leta
